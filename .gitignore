# Generated QR code cache — regenerated at runtime, never commit
phpqrcode/cache/

# 345 MB and unused — landingpage.html plays BackgroundAnimation.mp4 instead.
# Exceeds GitHub's 100 MB hard limit and would block every push.
assets/BackgroundAnimation.gif

# Editor / OS noise
.vscode/
.idea/
Thumbs.db
desktop.ini
*.swp
*~

# Logs and local dumps
*.log
*.sql.bak

# Uploaded parcel photos — user content, not source. Keep the folder, not the files.
uploads/parcels/*
!uploads/parcels/.gitkeep

# Dependencies, if Composer/npm are ever added
/vendor/
/node_modules/

# Local environment overrides
.env
.env.local
db_connect.local.php

# Holds the Supabase database password. This repo is public — never commit.
config.local.php

# Real seed rows — contain IC numbers and passwords. This repo is public.
supabase/seed.local.sql
